import * as vars from './../variables'

const toolbar = {
  container: {
    width: '100%',
    height: '100%',
    padding: '.38em'
  },
  firstButton: {
    marginLeft: vars.elementsGap
  }
}

export default toolbar